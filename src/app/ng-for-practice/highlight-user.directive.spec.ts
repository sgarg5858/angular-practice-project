import { HighlightUserDirective } from './highlight-user.directive';

describe('HighlightUserDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightUserDirective();
    expect(directive).toBeTruthy();
  });
});
